[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mi-ntk"          # what `pip list` will show
version = "0.0.1"

[tool.setuptools]
py-modules = ["tms", "ntk", "ma", "metrics"]
